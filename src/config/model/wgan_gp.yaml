name: wgan_gp
latent_dim: 128
generator:
  base_channels: 64
  output_channels: 3
  output_size: 64
critic:
  base_channels: 64
  input_channels: 3
  input_size: 64
  norm: layer  # layer | instance | none. Never batch — incompatible with GP.
